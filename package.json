{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "HOST=0.0.0.0 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "nuxt typecheck"
  },
  "devDependencies": {
    "nuxt": "^3.8.1"
  },
  "dependencies": {
    "@vueuse/core": "^10.6.1"
  }
}
